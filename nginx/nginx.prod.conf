upstream lcjs_servers {
    # 운영 환경에서는 실제 서비스 도메인/포트에 맞게 설정
    server lcjs-server1:3001 max_fails=3 fail_timeout=30s;
    server lcjs-server2:3002 max_fails=3 fail_timeout=30s;
    server lcjs-server3:3003 max_fails=3 fail_timeout=30s;
}

server {
    listen 80;

    server_name yourdomain.com;

    location / {
        proxy_pass http://lcjs_servers;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;

        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
